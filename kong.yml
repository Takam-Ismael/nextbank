_format_version: "3.0"
_transform: true

services:
  - name: accounts-service
    url: http://accounts-service:8085 # Matches the internal port of your Spring Boot app
    routes:
      - name: accounts-route
        paths:
          - /accounts
          - /api # Support both path versions
          - /auth

plugins:
  - name: cors
    config:
      origins:
        - "*"
      methods:
        - GET
        - POST
        - PUT
        - DELETE
        - PATCH
        - OPTIONS
      headers:
        - Accept
        - Authorization
        - Content-Type
      exposed_headers:
        - Authorization
      credentials: true
      max_age: 3600
